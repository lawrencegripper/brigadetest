# Testing [Brigade](https://brigade.sh/)

A simple project trying out brigade on Kubernetes. 

## Running: Cluster 

- Install brigade and helm in your cluster
- Run `helm install brigade/brigade-project --name brigade-test -f brig.yaml`